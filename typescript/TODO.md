# TODO

- finish config loader
  - Validator composer
  - Validator for lists
  - fallback JSON validation to string validation
  - JSON validators
  - loader for initial peers

- test networking
  - metrics endpoint?
  - run on Oracle?

- slices
